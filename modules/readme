This directory:  "radio" modules implementing custom wireless stack.
We are not using any wireless stack from NRF SDK.

Other modules used by (optionally) by app (not by wireless stack) found in platform dir.

